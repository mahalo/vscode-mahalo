<show id="marlo-brando"
      if=' user.firstName + user.lastName == "Marlo Brando" && true '>
    
    ${ user.firstName + 0.9 + 'ttt'} ${ user.lastName }: ${ 500.00 } ${ 'points' | translate }
    
    <button @click=' updatePoints(user) '></button>
</show>